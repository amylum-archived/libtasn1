item do
  expected do
    url_xpath
    url 'http://git.savannah.gnu.org/cgit/libtasn1.git/refs/tags'
    xpath '//*[@id="cgit"]/div[1]/table/tr[2]/td[1]/a'
    regex /^libtasn1_([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
